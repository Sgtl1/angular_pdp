import { WizardHousePipe } from './wizard-house.pipe';

describe('WizardHousePipe', () => {
  it('create an instance', () => {
    const pipe = new WizardHousePipe();
    expect(pipe).toBeTruthy();
  });
});
